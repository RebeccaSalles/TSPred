language: r
sudo: false
cache: packages

r:
 - oldrel
 - release
 - devel

r_packages:
  - covr

r_build_args: --resave-data

after_success:
  - Rscript -e 'library(covr); codecov()'
  - bash <(curl -s https://codecov.io/bash)
